pre-push:
  commands:
    typecheck:
      run: pnpm typecheck
    lint:
      run: pnpm lint
    format:
      run: |
        pnpm fmt
        git diff --quiet
      fail_text: "Code is not formatted! Stage and commit the changes."
# post-commit:
#   commands:
