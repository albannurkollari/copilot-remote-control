{
  "name": "@remote-copilot/relay-server",
  "version": "1.2.1",
  "private": true,
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "dev": "node --env-file=../../.env.dev src/main.ts",
    "build": "tsup",
    "clean": "rm -rfd dist",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "vitest run --project @remote-copilot/relay-server"
  },
  "dependencies": {
    "@remote-copilot/shared": "workspace:*",
    "picocolors": "^1.1.1",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1"
  }
}
