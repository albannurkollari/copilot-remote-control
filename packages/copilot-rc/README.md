# copilot-rc

CLI package for running the self-hosted Copilot Remote Control stack.

It provides one installable binary:

- `copilot-rc init` to create or update local configuration
- `copilot-rc start` to start the relay server and Discord bot together

The VS Code extension is intentionally published separately through the VS Code Marketplace.
